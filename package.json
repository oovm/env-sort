{
    "private": true,
    "scripts": {
        "build": "cargo build --release",
        "test": "cargo test --release",
        "fmt": "cargo fmt --all",
        "reset": "git reset Head~ --soft",
        "p": "git push && git push --prune --tags",
        "u": "cargo upgrade --workspace"
    }
}
